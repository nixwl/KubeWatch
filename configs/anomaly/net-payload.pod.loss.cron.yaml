apiVersion: v1
kind: ServiceAccount
metadata:
  name: net-payload-pod-loss-cron-runner
  namespace: chaosblade
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: net-payload-pod-loss-cron-runner
rules:
  - apiGroups: ["chaosblade.io"]
    resources: ["chaosblades"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: net-payload-pod-loss-cron-runner
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: net-payload-pod-loss-cron-runner
subjects:
  - kind: ServiceAccount
    name: net-payload-pod-loss-cron-runner
    namespace: chaosblade
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: net-payload-pod-loss-cron
  namespace: chaosblade
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  startingDeadlineSeconds: 60
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      backoffLimit: 0
      activeDeadlineSeconds: 12000
      ttlSecondsAfterFinished: 300
      template:
        spec:
          serviceAccountName: net-payload-pod-loss-cron-runner
          restartPolicy: Never
          containers:
            - name: runner
              image: bitnami/kubectl:latest
              imagePullPolicy: IfNotPresent
              envFrom:
                - configMapRef:
                    name: net-payload-env-pod-loss-configmap
              volumeMounts:
                - name: script
                  mountPath: /scripts
                - name: payload
                  mountPath: /payload
                - name: logs
                  mountPath: /logs
              command: ["/scripts/run.sh"]
          volumes:
            - name: script
              configMap:
                name: net-payload-script-pod-loss-configmap
                defaultMode: 0555
            - name: payload
              configMap:
                name: net-payload-template-pod-loss-configmap
                defaultMode: 0444
            - name: logs
              persistentVolumeClaim:
                claimName: node-logs-pvc
