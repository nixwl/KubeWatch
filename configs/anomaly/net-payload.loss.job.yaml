# net-payload.loss.job.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: net-payload-loss-job-runner
  namespace: chaosblade
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: net-payload-loss-job-runner
rules:
  - apiGroups: ["chaosblade.io"]
    resources: ["chaosblades"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: net-payload-loss-job-runner
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: net-payload-loss-job-runner
subjects:
  - kind: ServiceAccount
    name: net-payload-loss-job-runner
    namespace: chaosblade
---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: net-payload-loss-job-
  namespace: chaosblade
  labels:
    app: net-payload-loss
    run_type: manual
spec:
  backoffLimit: 0
  # Give enough time to cover the hold (recommend >= MAX_HOLD_SECONDS + TIMEOUT_PADDING + 60)
  activeDeadlineSeconds: 12000
  ttlSecondsAfterFinished: 300

  template:
    metadata:
      labels:
        app: net-payload-loss
        run_type: manual
    spec:
      serviceAccountName: net-payload-loss-job-runner
      restartPolicy: Never
      containers:
        - name: runner
          image: bitnami/kubectl
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: net-payload-env-loss-configmap
          volumeMounts:
            - name: script
              mountPath: /scripts
            - name: payload
              mountPath: /payload
            - name: logs
              mountPath: /logs
          command: ["/scripts/run.sh"]
      volumes:
        - name: script
          configMap:
            name: net-payload-script-loss-configmap
            defaultMode: 0555
        - name: payload
          configMap:
            name: net-payload-template-loss-configmap
            defaultMode: 0444
        - name: logs
          persistentVolumeClaim:
            claimName: node-logs-pvc
