apiVersion: batch/v1
kind: Job
metadata:
  name: k6-gitea-web-browse-job
  namespace: traffic
spec:
  backoffLimit: 0
  ttlSecondsAfterFinished: 3600
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: k6
          image: grafana/k6:0.49.0
          imagePullPolicy: IfNotPresent
          command: ["k6", "run", "/scripts/web_browse.js"]

          env:
            - name: BASE_URL
              value: "https://gitea.example.com"
            - name: INSECURE_TLS
              value: "true"
            - name: REPO
              value: "nixwl/tquic"
            - name: VUS
              value: "1"
            - name: DURATION
              value: "30s"
            - name: SLEEP_SECONDS
              value: "1"

            # Browse behavior parameters
            - name: EXPLORE_PAGES
              value: "2"
            - name: ENABLE_REPO_HOME
              value: "true"
            - name: ENABLE_ISSUES
              value: "true"
            - name: ENABLE_PULLS
              value: "true"
            - name: ENABLE_COMMITS
              value: "false"

            # thresholds
            - name: TH_FAIL_RATE_MAX
              value: "0.05"
            - name: TH_P95_MS_MAX
              value: "3000"
            - name: TH_CHECKS_MIN
              value: "0.95"

            # Secret injection parameters (token is used by setup to preload the repo pool; credentials are used for sites that require login)
            - name: GITEA_TOKEN
              valueFrom:
                secretKeyRef:
                  name: k6-gitea-secret
                  key: token
            - name: GITEA_USERNAME
              valueFrom:
                secretKeyRef:
                  name: k6-gitea-secret
                  key: username
            - name: GITEA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: k6-gitea-secret
                  key: password

          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "512Mi"

          volumeMounts:
            - name: k6-gitea-web-browse-scripts
              mountPath: /scripts
              readOnly: true

      volumes:
        - name: k6-gitea-web-browse-scripts
          configMap:
            name: k6-gitea-web-browse-scripts
            items:
              - key: web_browse.js
                path: web_browse.js
