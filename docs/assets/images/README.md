# Markdown Images

Store images in a dedicated subdirectory for each document.

## Naming Rule

Use a stable document slug as the folder name.

Examples:

- `kubernetes-deployment/`
- `cluster-bootstrap/`
- `storage-layout/`

This keeps Markdown image references predictable and avoids filename collisions.
