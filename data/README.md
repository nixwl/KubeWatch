# Data

This directory holds data assets and their manifests:

- `raw/`: original inputs
- `processed/`: final build outputs

The default `.gitignore` keeps most actual data out of git while preserving the directory layout and manifest docs.
