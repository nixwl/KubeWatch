# Templates

This directory stores copyable starting points, not concrete examples.

When you begin writing many Markdown docs or YAML files, copy from here first and then fill in the target content in `examples/` or other domain directories.
